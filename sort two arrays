Set<Integer> set = new LinkedHashSet<>();
        for (int num : a) {
            set.add(num);
        }
        for (int num : b) {
            set.add(num);
        }
        
        List<Integer> list = new ArrayList<>(set);
        Collections.sort(list);
        return list;
